# Form + settings

`FormLayout` / `ValidationSummary` account form plus a small `SettingsCard` preference block. Recipe: [`docs/forms.md`](../../docs/forms.md).

```bat
cmake --build build --config Release --target qwinui3_example_form
build\qwinui3_example_form.exe
```
