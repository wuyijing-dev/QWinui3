# SettingsCard settings page

Full-page settings built from `SettingsCard` and `SettingsExpander` (account, appearance, notifications).

```bat
cmake --build build --target qwinui3_example_settings
build\qwinui3_example_settings.exe
```
