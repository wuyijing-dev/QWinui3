import QtQuick
import QWinUI3.Extras

// AppBarPreset — semantic alias of CommandBarPreset for product shells.
//
// @notes
//   Keeps naming aligned with app developers (“AppBar”), while reusing the same
//   CommandBar defaults that avoid the common toolbar footguns.

CommandBarPreset {
    id: root
}

